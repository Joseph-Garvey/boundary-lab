[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "boundary-lab"
version = "0.4.2"
description = "Boundary element loudspeaker radiation solver"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beat-engine @ https://github.com/Joseph-Garvey/BEAT_Engine/releases/download/v0.2.1/beat_engine-0.2.1-py3-none-any.whl#sha256=7153c9b45c3c258ad45b59f97d86969edb4394ebac22e36813cc25fae29bc3e1",
  "gmsh",
  "matplotlib",
  "meshio",
  "numpy",
  "scipy",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "ruff>=0.15,<0.16",
]
gui = [
  "PySide6",
  "pyvista>=0.47",
  "pyvistaqt",
]

[project.scripts]
blab = "blab.cli:main"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/blab"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 120
src = ["src", "tests", "scripts"]
force-exclude = true
extend-exclude = [
  "ath/",
  "runs/",
  "references/",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"scripts/benchmark_gui_pipeline.py" = ["E402"]
